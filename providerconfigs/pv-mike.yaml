apiVersion: tf.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: providerconfig-mike
spec:
  credentials:
    - secretRef:
        key: creds
        name: aws-creds
        namespace: default
      source: Secret
